-- ============================================================================
-- Связь «вид расхода → источник/цель» (ТЗ §4.1.5, форма ЗРС): при выборе вида
-- расхода в форме заявки авто-подставляются Источник (фонд) и Цель расхода.
-- Сама привязка настраивается позже (значения по умолчанию на статью РД).
--
-- RLS на expense_types — из baseline (read_all + admin_write); новые колонки
-- наследуют существующие политики.
-- ============================================================================

alter table public.expense_types
  add column if not exists default_fund_id uuid references public.funds(id),
  add column if not exists default_purpose text;
