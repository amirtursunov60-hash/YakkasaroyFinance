-- ============================================================================
-- Включение pgTAP — модульное тестирование БД. Тесты инвариантов леджера лежат
-- в supabase/tests/ и запускаются через `supabase test db` (на ветке/staging),
-- а структурную часть можно прогнать и read-only на любой среде.
-- ============================================================================
create extension if not exists pgtap with schema extensions;
