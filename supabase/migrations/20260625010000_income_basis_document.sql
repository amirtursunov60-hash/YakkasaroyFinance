-- Доход §7 gap-map: документ-основание операции дохода как СТРУКТУРНОЕ поле.
-- Раньше его писали в свободный comment (плейсхолдер так и подсказывал) —
-- из-за чего нельзя было отдельно показать/искать/отчитываться по основанию.
-- Теперь — отдельная nullable-колонка (номер счёта/договора/чека/акта и т.п.).
--
-- Колонка наследует RLS таблицы incomes (insert: has_location_access + роль +
-- открытый период; чтение: has_location_access). Новых политик не требуется.
-- Баланс/Реестр не затрагиваются (поле справочное).

alter table public.incomes add column if not exists basis_document text;
comment on column public.incomes.basis_document is 'Документ-основание операции дохода (номер счёта/договора/чека/акта и т.п.) — структурное поле (ManaJet), отдельно от свободного comment';
