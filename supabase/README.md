# База данных (Supabase)

## Применение миграций

Два способа:

1. **Через Claude Code (Supabase MCP)** — основной. В корне репозитория есть `.mcp.json` с официальным Supabase MCP-сервером (https://mcp.supabase.com, авторизация через вход в Supabase — OAuth, токены не хранятся). После авторизации Claude применяет миграции (`apply_migration`) и выполняет диагностический SQL (`execute_sql`) сам. Внимание: параметр `read_only=true` в URL отключает запись — для применения миграций его быть не должно.
2. **Вручную**: Dashboard → SQL Editor → вставить содержимое файла → Run. По порядку номеров, каждый файл один раз (повторный запуск безопасен — файлы идемпотентны).

## Текущее состояние

**Базовая схема создана вне git** — вручную через Dashboard (июнь 2026, до подключения репозитория к процессу миграций). Это полная схема финконтура по ТЗ v2: 33 таблицы (`fp_periods`, `incomes`, `funds`, `fp_register`, `payment_requests`, `directives`, `reconciliations`, `counterparties`, оргсхема, статистики с квотами, `audit_log`, инвайты, права доступа по точкам/фондам/статьям) + enum-типы, функции прав и триггеры. SQL-дамп базовой схемы планируется добавить сюда как `000_baseline.sql` (справочно, не для выполнения).

Ключевые механизмы базовой схемы:

- **Реестр `fp_register`** — единая лента операций; триггер `fp_register_balances` поддерживает колонки `balance` фондов и счетов ДС, `fp_register_overdraft` запрещает уход в минус, `fp_register_period_lock` блокирует операции в закрытом периоде.
- Вставка в `incomes` автоматически порождает запись реестра (триггер `income_to_register`).
- Аудит: триггер `trg_audit` пишет изменения финансовых таблиц в `audit_log`.
- Права: RLS на функциях `my_role()`, `is_fin_admin()`, `has_location_access()`, `has_fund_access()`, `holds_position()` + таблицы персональных прав `user_location_access`, `fund_access`, `expense_type_access`.
- Везде `outer_id` (интеграции) и `is_archived` (архив вместо удаления); периоды — `starts_on`/`ends_on` (чт–ср).

## История миграций в этой папке

| Файл | Статус | Содержимое |
|---|---|---|
| `001_auth_profiles.sql` | применён | политики/функции поверх `profiles` — **отменён файлом 003** |
| `002_core.sql` | применён | дубли таблиц ядра (создание пропущено — таблицы уже были), лишние политики — **отменён файлом 003** |
| `003_cleanup.sql` | применён | удаляет объекты 001–002, дублировавшие и ослаблявшие базовую схему |
| `004_seed.sql` | применён | стартовые справочники прототипа: 6 точек, кассы и счета Алиф, фонды ФД1–ФД9/1, дерево D-кодов доходов, статьи РД (валюты и способы оплаты уже были залиты) |
| `005_fix_income_trigger.sql` | применён | приведение типа op_type в триггере trg_income_to_register — без него вставка дохода падает |
| `006_directive.sql` | применён | Директива: схема распределения по умолчанию (income_type_id nullable + сид 3 этапов) и функции fp_run_distribution / fp_close_period |
| `007_directive_stages.sql` | применён | поэтапное одобрение: fp_distribute_stage (этап в comment Реестра 'stage:…', включая перенос остатка 'stage:remainder'), fp_close_period помечает доходы распределёнными; fp_run_distribution удалена |
| `008_reopen_period.sql` | применён | fp_reopen_period — переоткрытие закрытой недели (протокол Директивы удаляется, при повторном закрытии создаётся заново) |
| `009_reset_and_autocreate.sql` | применён | fp_reset_distribution — сброс одобренного этапа/всего распределения (суммы списываются из фондов); fp_close_period теперь автосоздаёт следующую неделю |
| `20260615184000_ledger_concurrency_guards.sql` | **применён** | инварианты конкурентности леджера: fp_pay_request блокирует строку заявки `for update` + условный перевод статуса (защита от двойной оплаты), fp_distribute_stage блокирует строку периода `for update` (сериализация одобрения этапа), частичный UNIQUE-индекс `fp_register(request_id) where op_type='request_payment'` |
| `20260615185500_ledger_hardening_audit_reconcile.sql` | **применён** | доводка леджера: триггер `fp_register_no_update` (леджер неизменяем — UPDATE запрещён), функция `fp_reconcile_balances()` (сверка SUM(fp_register) = баланс фонда/счёта), аудит `fp_periods` и `directives` (переоткрытие/закрытие периода теперь пишутся в `audit_log`) |
| `20260615190000_fix_register_no_update_search_path.sql` | **применён** | фикс advisors `function_search_path_mutable`: зафиксирован `search_path = public` у `trg_register_no_update` |
| `20260615193000_enable_pgtap_testing.sql` | **применён** | включение pgTAP для модульного тестирования БД; тесты инвариантов леджера — в `supabase/tests/` |
| `20260616120000_org_chart.sql` | **применён** | оргсхема (ТЗ §4.3–4.4): в `org_divisions` — `color`, `ckp` + unique по `code`; в `org_positions` — `section`, `ckp`, `statistic`, `duties` (jsonb), `is_executive`, `sort`; enum `hat_status` (none/learning/done) + колонка в `position_assignments`; сидинг 7 отделений и постов прототипа (ЦКП, секции, шляпы) как стартовый справочник (RLS наследуется из baseline) |
| `20260616130000_request_form_fields.sql` | **применён** | форма заявки (ЗРС) по шаблону ManaJet: в `payment_requests` — `purpose` (цель расхода) и `tags` (метки, `text[]` not null default `{}`); период «К рассмотрению на ФП» использует существующую `period_id` (RLS наследуется из baseline) |
| `20260616140000_expense_type_defaults.sql` | **применён** | связь «вид расхода → источник/цель»: в `expense_types` — `default_fund_id` (uuid → `funds`) и `default_purpose` (text); форма ЗРС авто-подставляет фонд и цель при выборе вида расхода (привязка настраивается позже; RLS наследуется из baseline) |
| `20260616150000_request_decision_fields.sql` | **применён** | рассмотрение заявки в Директиве: в `payment_requests` — `approved_amount` (одобренная сумма, отдельно от запрошенной `planned_amount`; check `> 0` либо NULL) и `comment` (комментарий решения); `fp_pay_request` теперь оплачивает `coalesce(approved_amount, planned_amount)` (с блокировкой строки `for update`). RLS наследуется из baseline |
| `20260616160000_profile_avatar.sql` | **применён** | аватар сотрудника: `profiles.avatar_url` (text) + публичный бакет `avatars` с политиками (чтение всем, загрузка/замена — в свою папку по uid); self-update avatar_url разрешён политикой `profiles_self` |
| `20260616170000_avatar_admin_write.sql` | **применён** | админам (`is_fin_admin`) разрешена загрузка/замена аватара любого сотрудника (политики `avatars_insert`/`avatars_update` бакета `avatars` дополнены `OR is_fin_admin()`) |
| `20260618120000_funds_tab_fields_and_ops.sql` | **применён** | вкладка «Фонды» (`docs/funds-spec.md`): в `funds` — `description`, `color`, `stage` (один этап на фонд, бэкофилл из `distribution_rules`), `no_transfer` (запрет ручного перемещения/возврата), `is_private` (виден только owner/fin_director); enum `register_op_type` += `fund_income`/`fund_return`; RPC `fp_fund_income`/`fp_fund_return`; в `fp_fund_transfer`/`fp_fund_loan` — проверка `no_transfer`; в `fp_pay_request`/`fp_pay_bill` — запрет накопительного фонда; `has_fund_access` учитывает `is_private`. Применено двумя вызовами MCP (enum до использования) |
| `20260618121000_restore_pay_request_approved_amount.sql` | **применён** | восстановление `fp_pay_request` после предыдущей миграции: вернул блокировку строки `for update` (защита от двойной оплаты) и оплату по `coalesce(approved_amount, planned_amount)`, сохранив проверку накопительного фонда |
| `20260618130000_set_fund_stage.sql` | **применён** | `fp_set_fund_stage(fund, stage)`: смена этапа фонда переносит его в Директиве — обновляет/сворачивает дефолтное правило `distribution_rules` (income_type_id IS NULL) на новый этап (учитывает UNIQUE (fund_id, stage); ФД6 с двух этапов сворачивается в один). ФРС-правила по видам дохода не трогает. Используется `createFund`/`updateFund` |
| `20260618140000_fund_folder_fields.sql` | **применён** | папки фондов как карточки (`docs/funds-spec.md` §9): в `fund_folders` — `color`, `description`, `is_archived`. RLS не менялась (admin_write = is_fin_admin, чтение всем). Архив папки на клиенте: фонды внутри `folder_id → null`, папка `is_archived = true` |
| `20260618150000_fund_op_reverse.sql` | **применён** | откат операции фонда (`docs/funds-spec.md` §7): в `fp_register` — `reverses_id` (на откатываемую строку) + индекс; RPC `fp_reverse_fund_op(id)` добавляет компенсирующую запись (Реестр неизменяем, не удаляем), возвращая деньги в исходный фонд. Откатываются `fund_transfer`/`fund_income`/`fund_return`; запрет повторного отката; овердрафт/закрытый период держат триггеры |
| `20260618160000_reverse_loan.sql` | **применён** | `fp_reverse_fund_op` дополнен откатом займа: для `fund_loan` выполняется полный возврат через `fp_fund_loan_return` (деньги кредитору, долг закрывается). Откат в журнале вкладки доступен только в открытой неделе (статус периода ≠ closed — проверка на клиенте; в закрытом периоде вставку всё равно блокирует триггер) |
| `20260620210000_dashboard_tasks_bp.sql` | **применён** | Личный кабинет (перевод модуля `dashboard` с моков): enum `task_status` (new/progress/done) и `task_priority` (low/mid/high); таблицы `tasks` (поручения: постановщик `from_id`/исполнитель `to_id`, срок, статус, приоритет) и `battle_plan_items` (личное боевое планирование: `owner_id`, текст, цель, `done`, опц. `period_id`). RLS: задачи видят финадмин/постановщик/исполнитель; БП — личный список владельца. FK-индексы. Применено через Supabase MCP; `database.types.ts` перегенерирован |
| `20260620200000_crm_banquets.sql` | **применён** | CRM банкетов (перевод модуля `crm` с моков на БД): enum `crm_lead_stage` (new/show/offer/contract/won/lost); таблицы `crm_halls` (залы, сид справочника прототипа — 6 залов), `crm_clients` (база клиентов: tag/phone), `crm_leads` (воронка: зал, дата, гости, бюджет, этап, источник). RLS на родных функциях (`is_fin_admin`/`has_location_access`, сетевые записи при `location_id is null`); FK-индексы; `outer_id`/`is_archived` везде. Брони залов — производная от заявок (зал+дата+этап), отдельной таблицы нет. Применено через Supabase MCP; `database.types.ts` перегенерирован |
| `20260618170000_approve_funds_guard.sql` | **применён** | запрет одобрения сверх «Доступно» в фонде (принцип ТЗ «только фактически имеющиеся средства»): триггеры BEFORE INSERT/UPDATE на `payment_requests` и `supplier_bills` (`trg_request_approve_funds_check`/`trg_bill_approve_funds_check`) при переводе в `approved` проверяют, что сумма ≤ `funds.balance` − уже одобренные-неоплаченные (кроме самой строки). Суммы без валютной конвертации (как и отображаемые Остаток/Доступно) |
| `20260620230000_manajet_mirror.sql` | **применён** | Зеркало ManaJet для обкатки (read-only, `docs/manajet-анализ-и-интеграция.md`): таблицы `mj_funds`, `mj_periods`, `mj_purchase_orders` (заявки ЗРС с csw-полями), `mj_bills`, `mj_invoices`, `mj_incomes`, `mj_stats`, `mj_stat_values`, `mj_positions`, `mj_companies`, `mj_sync_log`. У каждой — плоские колонки для фильтров + сырой `data jsonb` + ключ `mj_id`/составной. RLS: SELECT только `is_fin_admin()`; запись делает Edge Function `manajet-sync` под сервис-ролью. Наполняется из боевого API ManaJet; **операционный финконтур не затрагивается**. Применено через Supabase MCP; `database.types.ts` перегенерирован. Сопутствующее (через `execute_sql`, не в миграции): секреты Vault `manajet_auth`/`mj_cron_secret`, RPC `mj_secret` (service_role-only), расширения `pg_net`/`pg_cron` |
| `20260620240000_statistics_ranges.sql` | **применён** | Коридор состояний у `statistics` (фикс §B5 анализа): добавляет `min_val`, `max_val`, `stat_type`, `sign`, `source` (идемпотентно). Нужно для состояний ХМС и для импорта определений статистик из ManaJet. pgTAP — `supabase/tests/statistics_ranges_test.sql`. Сопутствует Edge Function **`manajet-import-refs`** (импорт справочников ManaJet — FPFund→`funds`, IncomeCategory→`income_types`, ExpenseCategory→`expense_types`, Stat→`statistics` — прямо в операционные таблицы по `outer_id`; балансы/Реестр не трогает) и ночной cron `mj_import_refs`. Применено через Supabase MCP; `database.types.ts` перегенерирован. Импорт: income 31, expense 44, statistics 31 (28 с коридором), funds 115/115 (103 новых + 12 фондов-прототипов связаны по совпадению code+name — проставлен `outer_id`, с согласия заказчика; дублей нет, балансы не тронуты) |
| `20260620250000_mj_persons.sql` | **применён** | Зеркало сотрудников ManaJet `mj_persons` (read-only): в `profiles` людей ManaJet вставить нельзя (FK на `auth.users`), поэтому отдельный справочник как прочие `mj_*`. RLS — SELECT только `is_fin_admin()`. Наполняет `manajet-sync` (новая сущность `persons`, ~464). Посты ManaJet уже в `mj_positions` (540). Показ — read-only в модулях Сотрудники/Оргсхема (переключатель «Наши данные / ManaJet»); операционные `profiles`/`org_positions` не трогаются. pgTAP — `supabase/tests/mj_persons_test.sql`; cron `mj_sync_refs` дополнен `persons` |
| `20260623160000_reverse_bill_payment.sql` | **применён** | отмена оплаты счёта поставщика: RPC `fp_reverse_bill_payment(bigint)` добавляет компенсирующую запись в `fp_register` (фонд + и счёт ДС +, `reverses_id` на оплату — Реестр неизменяем) и возвращает счёт в `approved`, снимая `period_paid_id`. Права — финдиректор/владелец/бухгалтер; закрытую неделю оплаты сначала открыть; запрет повторной отмены. Тип записи-отмены — тот же `bill_payment` с плюсом (в отчётах гасится в ноль, в ленте «Операции со счетами» видна рядом). У `bill_payment` уникального индекса нет — вторая проводка допустима. pgTAP — `supabase/tests/reverse_bill_payment_test.sql`. Применён через Supabase MCP |
| `20260623150000_drop_request_payment_uniq.sql` | **применён** | снят жёсткий UNIQUE `fp_register_request_payment_uniq` («одна оплата на заявку», из 20260615184000) — он давал `duplicate key` при отмене оплаты и при повторной оплате после отмены (обе создают второй `request_payment` по заявке). Защита от двойной оплаты осталась в `fp_pay_request` (блокировка строки `FOR UPDATE` + условный перевод `approved`→`paid` с проверкой rowcount). pgTAP `reverse_request_payment_test.sql` дополнен `hasnt_index`. Применён через Supabase MCP |
| `20260623140000_reverse_request_payment.sql` | **применён** | отмена оплаты заявки: RPC `fp_reverse_request_payment(bigint)` добавляет компенсирующую запись в `fp_register` (фонд + и счёт ДС +, `reverses_id` на оплату — Реестр неизменяем) и возвращает заявку в `approved`. Права — финдиректор/владелец/бухгалтер; закрытую неделю оплаты сначала открыть; запрет повторной отмены. Тип записи-отмены — тот же `request_payment` с плюсом (в отчётах гасится в ноль, в ленте заявок видна рядом). pgTAP — `supabase/tests/reverse_request_payment_test.sql`. Применён через Supabase MCP |
| `20260623130000_request_period_guard_update.sql` | **применён** | расширение стража на UPDATE: триггер `request_period_open_check` теперь BEFORE INSERT OR UPDATE и блокирует не только подачу, но и **перенос** заявки в закрытую неделю (редактор заявки во вкладке «Заявки»). Срабатывает только когда `period_id` меняется на закрытый период (или это вставка) — прочие правки заявки проходят. pgTAP — `supabase/tests/request_period_open_test.sql` (4 проверки) |
| `20260623120000_request_period_open_guard.sql` | **применён** | запрет подачи заявки-ЗРС на закрытую неделю ФП (правило Директивы, ТЗ v2 §4): триггер BEFORE INSERT `request_period_open_check` (`trg_request_period_open_check`) на `payment_requests` не даёт вставить заявку с `period_id` закрытого периода (минуя UI). Только INSERT — решения финкомитета (UPDATE) не трогает; `period_id` nullable → при пустом периоде проверка пропускается. pgTAP — `supabase/tests/request_period_open_test.sql` |
| `20260624160000_request_partial_payments.sql` | **применён** | частичные оплаты заявок ЗРС: колонка `payment_requests.paid_amount` (накопленная оплата в валюте заявки, бэкофилл для оплаченных); `fp_pay_request` получил 4-й аргумент `p_amount` (по умолчанию = остаток → обратная совместимость), статус `paid` только при полной оплате, иначе остаётся `approved` (доплата); старая 3-арг перегрузка удалена (иначе неоднозначность PostgREST → HTTP 300); `fp_reverse_request_payment` уменьшает `paid_amount` (обратный пересчёт по `fx_rate`) и возвращает `approved`. Проверено на staging (частичная/переплата/полная/реверс + балансы). pgTAP — `supabase/tests/request_partial_payments_test.sql`. Применён через Supabase MCP; `database.types.ts` перегенерирован |
| `20260624170000_bill_partial_payments.sql` | **применён** | частичные оплаты счетов поставщиков (зеркало заявок): колонка `supplier_bills.paid_amount` (накопленная оплата, бэкофилл для оплаченных); `fp_pay_bill` получил 4-й аргумент `p_amount` (по умолчанию = остаток → обратная совместимость), статус `paid` только при полной оплате, иначе остаётся `approved` (доплата); старая 3-арг перегрузка удалена (иначе неоднозначность PostgREST → HTTP 300); `fp_reverse_bill_payment` уменьшает `paid_amount` (обратный пересчёт по `fx_rate`) и снимает `period_paid_id` при полном откате. Проверено на staging (частичная 250/600/переплата 400 отклонена/полная/реверс + баланс ФД1). pgTAP — `supabase/tests/bill_partial_payments_test.sql`. Применён через Supabase MCP; `database.types.ts` перегенерирован |
| `20260624180000_invoice_attachments_and_payment_reverse.sql` | **применён** | вложения у счёта клиента + откат отдельной оплаты клиента (Счета §3/§2 gap-map): таблица `invoice_attachments` (зеркало `bill_attachments` — договор/смета банкета, RLS read/insert/delete, индекс по `invoice_id`); колонка `incomes.reverses_income_id` (ссылка отмены на исходную операцию); RPC `fp_reverse_invoice_payment(uuid)` — сторно одной оплаты счёта клиента операцией дохода-возврата (`is_return=true`, триггер `trg_income_to_register` проводит `income_return` в Реестр, счёт ДС уменьшается), пересчитывает статус счёта; запрет повторной отмены и отмены возврата; закрытую неделю сначала открыть. Storage-бакет `attachments` уже покрывает путь `invoices/`. Проверено на staging (оплата 250+350 → возврат 250 → баланс ДС и статус счёта; оба стража). pgTAP — `supabase/tests/invoice_attachments_payment_reverse_test.sql`. Применён на staging и прод через Supabase MCP; `database.types.ts` обновлён вручную |
| `20260624190000_period_dual_confirmation.sql` | **применён** | два флага подтверждения периода ФП (Фонды §1 gap-map, модель ManaJet `is_executive_confirmed`/`is_baf_confirmed`): колонки `fp_periods.is_executive_confirmed`/`is_baf_confirmed` + `*_confirmed_at`/`*_confirmed_by` (бэкофилл закрытых периодов в true); RPC `fp_set_period_confirmation(uuid, text, boolean)` — ставит/снимает флаг (`executive` — owner/fin_director/ops_director; `baf` — финадмин; нельзя в закрытом периоде); `fp_close_period` теперь требует обоих подтверждений; `fp_reopen_period` сбрасывает оба флага. Статус `planning` (запрет подачи заявок) не затронут. Страж закрытия (`closeGuards.ts`) и UI Директивы показывают подтверждения как предусловие закрытия. Проверено на staging (закрытие без флагов / только исполнительное блокируется; оба → закрытие; переоткрытие сбрасывает). pgTAP — `supabase/tests/period_dual_confirmation_test.sql`. Применён на staging и прод через Supabase MCP; `database.types.ts` обновлён вручную |
| `20260624200000_request_payment_period.sql` | **применён** | период оплаты ≠ период планирования у заявки ЗРС (Заявки §3 gap-map, модель ManaJet `id_fp_plan`+`id_fp_plan_payment`; зеркало счетов с `period_approved_id`/`period_paid_id`): колонка `payment_requests.period_paid_id` (неделя оплаты; `period_id` остаётся неделей подачи/планирования) + индекс, бэкофилл из последней неотменённой проводки `request_payment`; `fp_pay_request` фиксирует `period_paid_id = p_period_id` (оплачивать можно в неделе, отличной от планирования); `fp_reverse_request_payment` снимает `period_paid_id` при полном откате. UI «Заявки»: в модале оплаты выбор недели оплаты (по умолчанию текущая), на карточке — «Неделя заявки» / «Неделя оплаты» при расхождении. Проверено на staging (одобрение в неделе 18.06, оплата 40+остаток в неделе 25.06 → period_paid_id=25.06; два реверса → period_paid_id=null). pgTAP — `supabase/tests/request_payment_period_test.sql`. Применён на staging и прод через Supabase MCP; `database.types.ts` обновлён вручную |
| `20260624210000_task_position_bp_links.sql` | **применён** | задача на ПОСТ оргсхемы + связь боевого планирования со статистикой/постом (Задачи §1/§2 gap-map, модель ManaJet `Task.id_orgboard_position`, BP `is_stats_visible`+посты): колонка `tasks.position_id` (+ индекс) — задача адресуется посту, RLS `tasks_read`/`tasks_update` дополнены `holds_position(position_id)` (держатель поста видит и ведёт); `battle_plan_items.statistic_id`/`position_id`/`is_stats_visible` (+ индексы) — пункт БП двигает конкретную статистику ИЦО и/или адресован посту. UI «Личный кабинет»: в форме задачи выбор поста-получателя, в форме БП — выбор статистики и поста (привязка к статистике ⇒ `is_stats_visible`), на карточках видно адрес-пост и статистику. pgTAP — `supabase/tests/task_position_bp_links_test.sql`. Применён на staging и прод через Supabase MCP; `database.types.ts` обновлён вручную |
| `20260624220000_counterparty_directory.sql` | **применён** | справочник контрагентов (Стат/Контр §16–18 gap-map): таблица `counterparty_categories` (категории, RLS read-all + запись финадмин/бухгалтер/управляющие) + колонка `counterparties.category_id` (+ индекс); таблица `counterparty_contacts` (несколько телефонов/почт: `kind`/`value`/`label`/`is_primary`, FK на контрагента `on delete cascade`, RLS read-all + запись/удаление финадмин/бухгалтер). Архив (§23) уже был — `is_archived`. UI: новый экран «Контрагенты» (модуль CRM) — карточки с поиском, фильтрами роль/категория, тумблером архива; формы контрагента (роли, телефон, ИНН, категория + создание категории на лету, комментарий) и добавления контактов; архив/возврат и удаление контакта. pgTAP — `supabase/tests/counterparty_directory_test.sql`. Применён на staging и прод через Supabase MCP; `database.types.ts` обновлён вручную |
| `20260624230000_reverse_income.sql` | **применён** | отмена операции дохода через сторно (Доход §2/§3 gap-map; прямой update/delete запрещён — неизменяемость Реестра): RPC `fp_reverse_income(uuid)` — обобщение `fp_reverse_invoice_payment` на любые операции дохода (продажи/ручной ввод); добавляет доход-возврат (`is_return=true`, `reverses_income_id`), триггер `trg_income_to_register` проводит `income_return` (счёт ДС уменьшается); для дохода со счётом клиента пересчитывает его статус; запрет повторной отмены и отмены возврата; закрытую неделю сначала открыть. UI «Доходы»: лента отдельных операций недели (ManaJet FpIncome) с отменой и правкой (правка = сторно + предзаполненная форма). pgTAP — `supabase/tests/reverse_income_test.sql`. Проверено на staging (доход 500 → сторно → баланс ДС; оба стража). Применён на staging и прод через Supabase MCP; `database.types.ts` обновлён вручную |
| `20260624240000_crm_workflow.sql` | **применён** | Workflow/Kanban CRM-воронки (Задачи §8/§9/§11/§15 gap-map): таблица `crm_stages` — настраиваемые колонки (имя/цвет/порядок/флаги `is_won`/`is_lost`, сетевые при `location_id is null`, RLS на родных функциях), засеяны 6 стартовых из прежнего enum; `crm_leads.stage_id` (бэкофилл по legacy-коду) + `due_date`/`responsible_id`/`sort`; таблица `crm_lead_checklist` (чек-лист карточки, RLS через доступ к лиду). enum `crm_lead_stage` оставлен для обратной совместимости. UI (модуль CRM): воронка переписана в **Kanban-доску** — колонки из `crm_stages` (горизонтальный скролл), drag-and-drop на десктопе + select-перенос на телефоне; карточка-детали (этап, ответственный, срок, чек-лист, заметка); управление колонками (добавить/переименовать/цвет/порядок/флаги won-lost/архив). Брони и База клиентов переведены на флаги стадий. pgTAP — `supabase/tests/crm_workflow_test.sql`. Бэкофилл проверен на staging (4/4 лида). Применён на staging и прод через Supabase MCP; `database.types.ts` обновлён вручную |
| `20260625000000_request_status_withdrawn.sql` | **применён** | Заявки §7 gap-map (шаг 1/2): значение `withdrawn` добавлено в enum `request_status` (общий для `payment_requests`/`supplier_bills`/`payroll_sheets`) — статус «отозвана» ≠ «отклонена» (rejected). Вынесено отдельной миграцией, т.к. в PostgreSQL новое значение enum нельзя использовать в той же транзакции, где оно добавлено. Применён через Supabase MCP |
| `20260625000100_fp_withdraw_request.sql` | **применён** | Заявки §7 gap-map (шаг 2/2): RPC `fp_withdraw_request(uuid)` (SECURITY DEFINER) — заявитель отзывает свою заявку, пока она «подана» (submitted), со своими проверками (владелец + статус); переводит в `withdrawn`. RPC, а не прямой update, т.к. политика `requests_update` отвергла бы новую строку (USING требует submitted). Триггер-страж периода не мешает (меняется только status). UI «Заявки»: кнопка «Отозвать» на своей поданной заявке (с подтверждением), статус «отозвана» (C.sub). Проверено вживую на проде (владелец→withdrawn; чужой→отказ; не-submitted→отказ; всё в откатываемой транзакции). pgTAP — `supabase/tests/request_withdrawn_test.sql`. Применён через Supabase MCP; `database.types.ts` обновлён вручную |
| `20260625020000_request_status_revision.sql` | **применён** | Заявки §8 gap-map (шаг 1/2): значение `revision` («на доработке») в общий enum `request_status` — финкомитет возвращает заявку автору на правку (≠ отклонение). Отдельной миграцией (enum-значение нельзя использовать в той же транзакции). Применён через Supabase MCP |
| `20260625030000_request_comment_ai.sql` | **применён** | ИИ-рецензент ЗРС в треде заявки: `request_comments.author_id` стал nullable + флаг `is_ai`. ИИ-комментарии пишет Edge Function **`request-ai-review`** под сервис-ролью (author_id=null, is_ai=true). Функция читает ЗРС заявки, зовёт **Claude API** (ключ из Vault: `vault.create_secret('<key>','anthropic_api_key')`; модель — секрет `anthropic_model`, по умолчанию `claude-haiku-4-5-20251001`) и при неполноте/вопросах пишет комментарий автору; полная заявка → молчит. **Диалоговый режим (v4):** функция читает ЗРС + всю переписку и отвечает на последнее сообщение автора; вызывается клиентом fire-and-forget после подачи/переподачи заявки И после каждого комментария человека в треде (`api.requestAiReview`). Сам себя не триггерит (ИИ-вставка под сервис-ролью) — зацикливания нет; потолок 25 ответов/заявку + антидубль 30с. Комментарий подаётся **от лица «Финансового директора»** (персона в системном промпте; без «ИИ»-маркировки). Ключ — `anthropic_api_key` в Vault (добавлен), модель — `anthropic_model` (по умолч. `claude-haiku-4-5-20251001`). UI: реплика в треде помечена «💼 Финансовый директор» (C.green); тред делает отложенные перезагрузки, чтобы ответ появлялся сам. Деплой через Supabase MCP (verify_jwt=true). pgTAP — `supabase/tests/request_comment_ai_test.sql`; `database.types.ts` обновлён вручную. **Текущие настройки в Vault (правятся без передеплоя):** `anthropic_model` = `claude-sonnet-4-6` (грамотный таджикский), `ai_review_prompt` = живой системный промпт (краткие ответы, язык сотрудника: таджикский ≠ узбекский; переопределяет встроенный дефолт функции). UI треда: галочки статусов (отправлено/доставлено/прочитано), индикатор «печатает», задержка ответа 9–15с, аватарки участников. |
| `20260626000000_notifications.sql` | **применён** | In-app уведомления (Задачи §12 gap-map): таблица `notifications` (user_id/kind/title/body/module/view_key/request_id/is_read), RLS — пользователь видит и помечает прочитанными только свои (insert только триггерами). Наполнение централизовано триггерами БД (без правки edge-функции): `trg_notify_request_comment` (AFTER INSERT на `request_comments` — уведомляет автора заявки о новом комментарии, в т.ч. ответе Финдиректора; не уведомляет о своём же), `trg_notify_request_decision` (AFTER UPDATE OF status на `payment_requests` — одобрена/отклонена/возвращена/оплачена). UI: `NotifyBell` в шапке переписан в реальную ленту (непрочитанные бейджем, дропдаун со списком, «прочитать все», клик ведёт в раздел + помечает прочитанным); `api.fetchNotifications`/`markNotificationsRead`. Проверено на проде (вставка ИИ-комментария → 1 уведомление автору, в откатываемой транзакции). pgTAP — `supabase/tests/notifications_test.sql`; `database.types.ts` обновлён вручную. _Остаётся из §12/§13: массовые рассылки клиентам (Massmail)._ |
| `20260625020100_request_revision_policy.sql` | **применён** | Заявки §8 gap-map (шаг 2/2): RLS — ветка автора в политике `requests_update` расширена с `status='submitted'` до `status in ('submitted','revision')`, чтобы автор мог отредактировать и подать заново возвращённую заявку (revision→submitted). Возврат на доработку делает финкомитет обычным UPDATE (ветка `is_fin_admin()` уже разрешает; в коде — `decideRequest` со `status='revision'`, причина в `rejection_reason`), RPC не нужен. UI: кнопка «На доработку» в Директиве (`RequestReviewControls`), статус «на доработке» (C.gold) + чип-фильтр, показ заметки автору, правка автором подаёт заново (очищает причину). Структурно проверено на проде (enum + текст политики). pgTAP — `supabase/tests/request_revision_test.sql`. Применён через Supabase MCP; `database.types.ts` обновлён вручную |
| `20260625010000_income_basis_document.sql` | **применён** | Доход §7 gap-map: документ-основание операции дохода как СТРУКТУРНОЕ поле — nullable-колонка `incomes.basis_document` (№ счёта/договора/чека/акта), отдельно от свободного `comment` (раньше основание писали в комментарий). Наследует RLS таблицы `incomes`, баланс/Реестр не затрагивает. UI «Доходы»: отдельное поле в форме операции (плейсхолдер «№ счёта / договора / чека / акта…»), показ на карточке операции (иконка-документ), перенос в предзаполнение при правке (сторно+форма); из плейсхолдера «Комментарий» убрано «документ-основание». pgTAP — `supabase/tests/income_basis_document_test.sql`. Применён через Supabase MCP; `database.types.ts` обновлён вручную |
| `20260626120000_counterparty_card.sql` | **применён** | Расширенная карточка контрагента (gap-map Контрагенты): аддитивные nullable-колонки `counterparties.entity_type` (физлицо/юрлицо, CHECK `individual`/`legal`/NULL), `address`, `bank_name`, `bank_account`, `bank_mfo`, `contact_person` (ответственное лицо). Наследуют RLS таблицы; существующие записи не трогаются. API: `createCounterpartyFull`/`fetchCounterpartiesFull` дополнены реквизитами, `updateCounterparty` уже принимает произвольный patch. UI «Контрагенты» (CRM): в форме — тип, ответственное лицо, адрес, банковский блок (банк/р.с/МФО); на карточке — бейдж типа, ответственное лицо, адрес, реквизиты с иконками. pgTAP — `supabase/tests/counterparty_card_test.sql`. Применён на прод через Supabase MCP; `database.types.ts` обновлён вручную |


## Тесты БД (pgTAP)

`supabase/tests/ledger_invariants_test.sql` — pgTAP-набор на инварианты леджера (существование Реестра/функций, триггеры неизменяемости/овердрафта/блокировки периода, unique-индекс против двойной оплаты, отсутствие UPDATE-политики на `fp_register`, аудит reopen/close, сверка `SUM(fp_register)` = баланс). Запуск: `supabase test db` (на ветке/staging) или `pg_prove`. Структурная + read-only часть (15 проверок) безопасна на любой среде, включая прод; поведенческие тесты с фикстурами (овердрафт/двойная оплата/закрытый период) — секция TODO в файле, запускать на ветке/staging.

`supabase/tests/org_chart_test.sql` — pgTAP на структуру оргсхемы (новые колонки отделений/постов, enum `hat_status`, наличие засеянного справочника: ≥ 7 отделений, ровно 7 руководящих постов). Только структурные/справочные проверки (11), ничего не пишут — безопасно везде.

`supabase/tests/request_form_fields_test.sql` — pgTAP на поля формы заявки (`payment_requests.purpose`, `tags` — тип `text[]`, NOT NULL). 4 структурные проверки, ничего не пишут.

`supabase/tests/expense_type_defaults_test.sql` — pgTAP на значения по умолчанию вида расхода (`expense_types.default_fund_id` — uuid, `default_purpose`). 3 структурные проверки, ничего не пишут.

`supabase/tests/request_decision_fields_test.sql` — pgTAP на поля рассмотрения заявки (`payment_requests.approved_amount` — numeric, `comment`; наличие `fp_pay_request`). 5 структурных проверок, ничего не пишут.

`supabase/tests/profile_avatar_test.sql` — pgTAP на аватар (`profiles.avatar_url`, наличие публичного бакета `avatars`). 2 проверки, ничего не пишут.

Файлы 001–002 оставлены как история применённого; выполнять их повторно не нужно. Прежние `003_finance.sql`/`004_seed.sql` к базе не применились (откатились с ошибкой) и заменены.

Правила распределения (`distribution_rules`) не сидируются: в схеме правило обязательно привязано к виду дохода (`income_type_id not null`), способ привязки стартовой схемы 3 этапов решается при реализации Директивы.

## Правила

- Любое изменение схемы — **только новым файлом миграции** (004, 005, …); применённые файлы не редактировать.
- Новые таблицы — всегда с RLS на родных функциях прав (`my_role()`, `is_fin_admin()`, `has_location_access()`...), с `outer_id` и `is_archived`.
- Балансы руками не трогать — только через операции в `fp_register` (их поддерживают триггеры).
- Стиль идемпотентности: `if not exists` / `on conflict` / `drop ... if exists`.
