// Превью Login — экран входа в кабинет «Яккасарой финанс».
// Колбэк onEnter — пустышка (вызывается только при успешном входе).
// Форма (email/пароль/показать пароль/кнопка) рендерится автономно; реальный
// вход бьёт в Supabase Auth только по сабмиту, поэтому статичный рендер безопасен.
import { Login } from "yakkasaroy-management";

export function SignIn() {
  return <Login onEnter={() => {}} />;
}
